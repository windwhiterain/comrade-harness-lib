{
  "name": "comrade-harness-lib",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "comrade-harness 节点库：节点（buildSystemPrompt/callLLM/runTools/…）+ 子图（loadContext/agentLoop/saveTurn/standardFlow，逐层可定制）+ 资源（LLM/记忆/工具）+ 运行时壳（createHarness）。harness 数据流默认 = standardFlow，core 可逐层深入定制或自写",
  "files": ["*.ts", "tsconfig.json", "ui"],
  "exports": {
    ".": "./index.ts"
  }
}
